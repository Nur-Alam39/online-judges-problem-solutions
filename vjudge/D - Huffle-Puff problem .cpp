#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,p;
    scanf("%d%d",&n,&p);
    string a[n];
    cin.ignore();
    for(int i=0;i<n;i++)getline(cin,a[i]);
    cout<<p;
    return 0;
}
