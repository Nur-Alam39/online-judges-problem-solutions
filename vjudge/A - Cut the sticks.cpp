#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n;
    scanf("%d",&n);
    vector<int>r;
    for(int i=0; i<n; i++)
        scanf("%d",&x),r.push_back(x);
    sort(r.begin(),r.end());
    while(r.size()>1)
    {
        for(int i=0; i<r.size(); i++)
        {

        }
    }
    return 0;
}
