#include<bits/stdc++.h>
#define Max 1002
using namespace std;
int R,C,r,grid[Max][Max],visit[Max][Max];
int main()
{
    while(scanf("%d%d",&R,&c)&&(R&&C))
    {
        scanf("%d",&r);
        int x,y;
        for(int i=0;i<r;i++)
        {
            scanf("%d",&x);
            int c;
            scanf("%d",&c);
            for(int j=0;j<c;j++)
            {
                scanf("%d",&y);
                grid[x][y]=-1;
            }
        }
        b
    }
    return 0;
}
