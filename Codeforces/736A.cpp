#include<bits/stdc++.h>
using namespace std;

int main() {
    int tc;
    scanf("%d", &tc);
    while(tc--) {
        int p;
        scanf("%d", &p);
        printf("2 %d\n", p - 1);
    }
    return 0;
}
