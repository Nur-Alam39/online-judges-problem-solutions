#include<bits/stdc++.h>
using namespace std;
int main()
{
    string a;
    while(cin>>a)
    {
        if(a=="never"||a=="be"||a=="so"||a=="happy"||a=="when"||a=="you"||a=="are"||a=="happy"||a=="and"||a=="never"||a=="be"||a=="so"||a=="sad"||a=="when"||a=="you"||a=="are"||a=="sad")
            printf("YES\n");
            else printf("NO\n");
    }
    return 0;
}

