#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,m,tc,q,l;
    scanf("%d",&tc);
    whle(tc--)
    {
        scanf("%d%d",&n,&m);
        char a[n+1],b[m+1];
        scanf("%s%s",&a,&b);
        scanf("%d",&q);
        while(q--)
        {
            scanf("%d",&l);
            for(int i=0;i<)
        }
    }
    return 0;
}
