#include<bits/stdc++.h>
using namespace std;
int main()
{
    int tc;
    scanf("%d",&tc);
    cin.ignore();
    while(tc--)
    {
        string s,t;
        getline(cin,s),getline(cin,t);
        while(1)
        {

        }
    }
    return 0;
}
